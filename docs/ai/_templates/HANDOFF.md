# Handoff

No implementation handoff is active.

Project memory was initialized for `<Project Name>` on `<YYYY-MM-DD>`. Continue from
[CURRENT.md](CURRENT.md) and load stable memory only for the selected task. Inspect Git directly
before editing.
