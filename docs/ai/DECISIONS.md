# Decisions

| Date       | Decision                                                                                                     | Reason                                                                                                                                                  | Alternatives considered                                                                         | Scope                                             | Status     |
| ---------- | ------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- | ------------------------------------------------- | ---------- |
| 2026-07-04 | Use `docs/ai/` as the source of truth for project memory.                                                    | User requested repo-versioned Markdown memory with stable and operational files.                                                                        | Store memory in `AGENTS.md`; external/private notes.                                            | Documentation and agent workflow.                 | Accepted   |
| 2026-07-04 | Keep `AGENTS.md` as a routing file instead of a knowledge dump.                                              | Top-level instructions should stay short and point agents to the correct memory files.                                                                  | Copy product/architecture facts into `AGENTS.md`.                                               | Agent onboarding.                                 | Accepted   |
| 2026-07-04 | Use `.agents/skills` for memory procedures.                                                                  | The repo already uses `.agents/skills`; creating a duplicate skill system would add confusion.                                                          | Add scripts only; create another skills directory.                                              | Agent workflow.                                   | Accepted   |
| 2026-07-04 | Keep product/domain memory separate from current task state.                                                 | Stable facts and operational state age differently and should not overwrite each other.                                                                 | Single large memory file.                                                                       | Memory taxonomy.                                  | Accepted   |
| 2026-07-04 | Use `docs/adr/` for formal ADRs and `docs/ai/DECISIONS.md` for compact decisions.                            | No ADR system existed, but future durable architecture choices may need fuller records.                                                                 | Only compact table; only ADR files.                                                             | Decision tracking.                                | Accepted   |
| 2026-07-04 | Use `plans/*.md` for multi-session feature plans.                                                            | Plans need a separate lifecycle from current dashboard and durable memory.                                                                              | Put active plans in `CURRENT.md`; use external task tracking only.                              | Work planning.                                    | Accepted   |
| 2026-07-04 | Add a starter project-memory initialization workflow.                                                        | Derived projects need clean project-instance memory without losing starter technical conventions.                                                       | Manually edit memory each time; keep one shared memory state.                                   | Starter workflow and memory taxonomy.             | Accepted   |
| 2026-07-04 | Rename code convention skills to `frontend-*`.                                                               | These skills apply to the Expo app and frontend business core, and should not be confused with backend/server skills in a future monorepo.              | Keep generic skill names; use `front-*` shorthand.                                              | Local skills and agent routing.                   | Accepted   |
| 2026-07-04 | Do not keep a standalone starter-only memory file.                                                           | Reusable starter knowledge already belongs in architecture, technical, testing, and skill docs; a separate file adds reset complexity.                  | Keep a starter-only file and remove it during initialization.                                   | Starter initialization workflow.                  | Accepted   |
| 2026-08-19 | Use typed bounded-context `Result` contracts for every fallible frontend gateway operation.                  | Expected failures must be explicit, infrastructure-independent, and propagate unchanged through RTK Query to the UI.                                    | Raw exceptions; success/failure response unions; stored user-facing messages.                   | Frontend business core.                           | Accepted   |
| 2026-08-19 | Keep transient request failures in RTK Query instead of durable slices.                                      | Request state belongs to the request lifecycle unless the failure itself is durable product truth.                                                      | Store the latest error object or message in each context slice.                                 | Runtime state and UI error handling.              | Accepted   |
| 2026-08-19 | Synchronize reusable RVA patterns without copying product-specific implementation.                           | The starter should inherit proven architecture while remaining a neutral template.                                                                      | Wholesale copy of RVA skills and contexts; documentation-only synchronization.                  | Starter skills and reference implementations.     | Accepted   |
| 2026-08-19 | Keep Redux auth as the single runtime session truth and persist credentials only through SecureStore.        | Duplicate persisted connection state caused startup races, while AsyncStorage and RTK Query persistence exposed tokens.                                 | Persist the full Redux tree; keep a Zustand connection flag; nested Redux persistence for auth. | Authentication, persistence, and startup routing. | Superseded |
| 2026-08-19 | Use RVA's secure Redux Persist storage algorithm for authentication credentials.                             | The starter should demonstrate the same production-proven token extraction, recursive sanitization, serialized writes, and rehydration behavior as RVA. | Starter-specific `SessionStorage` gateway/decorator/bootstrap; persist only an allowlist.       | Authentication persistence runtime.               | Accepted   |
| 2026-08-19 | Bundle typed French and English i18next catalogs with French as the fallback locale.                         | Starter needs a reusable localization baseline aligned with RVA while remaining independent from tenant-specific generated resources.                   | Hard-coded UI copy; copy RVA tenant localization wholesale; use English fallback.               | Presentation runtime and copy.                    | Accepted   |
| 2026-08-19 | Keep the remote auth example behind an opt-in HTTP adapter with an injected Redux session provider.          | Starter needs a complete transport-to-UI error reference without inventing a mandatory production backend or duplicating session state.                 | Make HTTP the default; pass tokens through use-cases; retain credentials inside the adapter.    | Auth infrastructure and runtime composition.      | Accepted   |
| 2026-08-20 | Let use-cases call their bounded-context gateway directly and adapt `Result` values with `toRtkQueryResult`. | HTTP-shaped base-query requests leaked `url` and `method` into use-cases and duplicated infrastructure routing.                                         | Semantic base-query commands; keep internal request routing; per-use-case manual conversion.    | Auth, subscription, and RTK Query integration.    | Accepted   |
| 2026-08-20 | Inject a shared `DateProvider` into time-dependent frontend adapters.                                        | Runtime behavior needs the real clock while in-memory adapters and behavior specs need deterministic timestamps.                                        | Direct `Date.now()` and `new Date()` calls; fake timers.                                        | Shared frontend infrastructure and adapters.      | Accepted   |
