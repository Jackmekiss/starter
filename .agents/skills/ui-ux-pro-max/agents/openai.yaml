interface:
  display_name: "UI UX Pro Max — Native"
  short_description: "Explore native app visual and UX directions"
  default_prompt: "Use $ui-ux-pro-max to explore and specify a strong native design direction before implementation."
policy:
  allow_implicit_invocation: true
